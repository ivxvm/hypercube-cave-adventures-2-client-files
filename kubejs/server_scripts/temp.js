                // event.player.tell(JSON.stringify(
                //     event.player.inventory.getSlotWithRemainingSpace(0)));
                // event.player.tell(JSON.stringify(
                //     event.player.inventory.compartments));

                // event.player.tell(JSON.stringify(
                //     event.player.inventory.getContainerSize()));
                // event.player.tell(JSON.stringify(
                //     event.player.inventory.countNonEmpty()));
                // event.player.tell(JSON.stringify(
                //     event.player.inventory.getContainerSize() - event.player.inventory.countNonEmpty()));
                // event.player.tell(JSON.stringify(
                //     Object.keys(event.player.inventory))); // .filter(k => k.toLowerCase().includes("slot") || k.toLowerCase().includes("inven"))));
